#vector name "assignment2"
assignment2 <- c(16, 18, 14, 22, 27, 17, 19, 17, 17, 22, 20, 22)

#function "myMean" takes a vector as an argument and returns the mean
myMean <- function(assignment2) {
  return(sum(assignment) / length(someData))
}

# attempt to use "myMean" function on the vector "assignment2"
myMean(assignment2)